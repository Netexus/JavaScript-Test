let passwords = [];
let password = ""
let regex = /\d/;

// do {
//     password = prompt("Enter your password (At least 1 number and 8 characters)")
// } while (regex.test(password));
// passwords = password 
// alert(passwords)
// password.length !== "8" && !